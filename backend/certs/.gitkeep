Place your organization's corporate root CA certificate here (e.g. corporate-root.crt) so
`docker build` can trust it when running `npm install` behind a TLS-inspecting proxy.
Any *.crt files in this directory are picked up automatically; it's a no-op if empty.
See the "Building behind a TLS-inspecting corporate proxy" section in the root README.md.
